# Gate password for the whole site (entered on /login).
APP_PASSWORD=

# Neon Postgres connection string (provided by the Vercel Neon integration;
# pull locally with `vercel env pull .env.local`).
POSTGRES_URL=
